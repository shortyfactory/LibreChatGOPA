services:
  api:
    environment:
      - NODE_ENV=development
      - DOMAIN_CLIENT=http://localhost
      - DOMAIN_SERVER=http://localhost
      - ALLOW_EMAIL_LOGIN=true
      - ALLOW_SOCIAL_LOGIN=true
      - OPENID_AUTO_REDIRECT=false
      - OPENID_POST_LOGOUT_REDIRECT_URI=http://localhost/login
